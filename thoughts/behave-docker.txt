name: Running behave with a docker image
tage: docker behave gssutils testing test nose unit

# With gssuilts just do this
docker run -v /var/run/docker.sock:/var/run/docker.sock -v $PWD:/workspace -w /workspace python:3.7 sh ./test.sh

# For anything else, you'll need to replace `sh ./test/sh` with an actual behave command
# eg: 
# behave -n "My Scenario"
