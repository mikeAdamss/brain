tags: github gssutils gss-utils gss bump release
name: bump gss utils release version

• git tag -m ‘Some message’ -a v0.5.something-bumped-up
• git push --follow-tags
• update the databaker-docker/requirements.txt file to point to the new version
• git commit/push databaker-docker
• wait 10 mins for hub.docker.com to build the updated image
• docker pull gsscogs/databaker on ci.floop.org.uk to get the latest versions on Jenkins.
